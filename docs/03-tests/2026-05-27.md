---
tags: [vlm, perception, tests]
date: 2026-05-27
summary: VLM 感知模块测试报告 — 24 passed / 0 failed，安装 Pillow 依赖后全绿
status: done
---

## [测试背景]

按照 `.claude/plans/3-3d-locate-wise-axolotl.md` 计划完成 VLM 感知能力开发后，运行测试验证。

## [第一轮测试] 缺少依赖

| 测试文件 | 用例数 | 结果 | 说明 |
|----------|--------|------|------|
| `test_perception.py` | 24 | ❌ 6 failed/error | 缺少 `PIL` 模块 |

### 失败原因
- `ModuleNotFoundError: No module named 'PIL'`
- `fake_perception.py` 的 `_fake_capture()` 方法需要 Pillow 生成假图像

## [修复] 安装 Pillow

```bash
.venv/bin/pip install Pillow
```

## [第二轮测试] 全通过

| 测试类 | 用例数 | 结果 |
|--------|--------|------|
| `TestFakeVlmPerception` | 5 | ✅ |
| `TestDeprojection` | 4 | ✅ |
| `TestHookRegistry` | 6 | ✅ |
| `TestCaptureResult` | 2 | ✅ |
| `TestJsonParsing` | 4 | ✅ |
| `TestHookRuleParsing` | 2 | ✅ |
| `TestFakeVlmPerceptionIsolation` | 1 | ✅ |
| **合计** | **24** | **全部通过** |

## 验证项完成情况

- [x] `FakeVlmPerception` 返回 mock 数据，不崩溃
- [x] `observe()` 返回结构化 JSON
- [x] `locate()` 返回 3D 坐标
- [x] 深度反投影 `deproject_2d_to_3d()` 计算正确
- [x] Hook 注册表正确加载和过滤
- [x] JSON 解析处理各种格式

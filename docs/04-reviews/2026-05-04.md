---
tags: [gatekeeper, review, phase2, p0-fix]
date: 2026-05-04
summary: Phase 2 Gatekeeper 审查 — P0-1 死代码/策略缺口修复 + P0-2 空白字符绕过修复
status: done
---

# 2026-05-04 — Gatekeeper 审查

## P0-1: _has_file_write_intent 死代码，策略执行缺口
- 删除 exec_tools.py 中未使用的 _has_file_write_intent()
- ToolGuard._would_write_files() 扩展为检测 cp/mv/touch/mkdir/dd of=/install/tee
- 追加检测 python 代码含 open()/write_text/write_bytes 的文件写入意图
- 修复后：file_write_require_approval=True 时，所有文件写入命令均触发审批

## P0-2: FORBIDDEN_CMDS 空白字符绕过
- 新增 _normalize_whitespace() — re.sub(r"\s+", " ", s) 将连续空白折叠为单空格
- _is_safe() 先归一化再匹配，消除 rm -rf  /、rm\t-rf\t/、dd  if= 等绕过
- 验证 5 种绕过全部拦截

---
tags: [phase1, gatekeeper, review]
date: 2026-04-28
summary: Phase 1 全部 10 个 Section 的 Gatekeeper 审查记录，含 P0/P1 修复
status: done
---

# 2026-04-28 — Gatekeeper 审查记录

## Section 1 审查 → PASS
- P1：测试只有 import 无断言 → 新增 tests/test_models.py（12 条断言全部通过）
- P2：api_key 可用 SecretStr、WorkspaceLimits 缺 max>min validator、Artifact.kind 可改为 Enum

## Section 2 审查 → PASS
- P1-1：未知 slash 命令静默发送给 LLM → 返回 "未知命令" 提示
- P1-2：4 个测试断言太弱 → 强化为验证具体内容

## Section 3 审查 → PASS
- P1（6 项全修）：
  1. tool_use buffer key 从 tc.id 改为 tc.index
  2. AgentLoop provider=None guard → provider 改为必填参数
  3. 工具执行异常加入 structlog 日志
  4. ContextMemory 移除 5 个未用字段
  5. 工具 schema 构建去重 → 分离 tool_handlers 和 tool_schemas
  6. unhandled finish_reason 返回 error event

## Section 4 审查 → FAIL → PASS
- 第 1 次 FAIL：3 P0
  - P0-1：check_operation() float() 无保护 → 加 _safe_float()
  - P0-2：状态转换无守卫 → 加 RuntimeError guard
  - P0-3：重试语义 off-by-one → 改为 > + last_error + recover()
- 第 2 次 FAIL：测试类名重复 → 6 个测试被吞 → 修复

## Section 5 审查 → FAIL → PASS
- 第 1 次 FAIL：4 P1
  - test_gripper_on_off 零断言 → 加断言 + 错误注入
  - get_status/health_check 异常路径未测 → RaisingClient 测试
  - FakeRobotBackend 缺 shutdown → 已补

## Section 6 审查 → PASS
- 4 P1 快速修复：去未用 imports + 合并重复 L2 测试 + 加关节边界测试 + 展开 profile 断言

## Section 7 审查 → FAIL → PASS
- P0: move_robot_home 安全校验缺失 → 已加
- P1: emergency_stop bool("false")→True → 改为 isinstance 严格校验

## Section 8 审查 → FAIL → PASS
- P0：沙箱参数名 mismatch → 对齐 move_xyz_rotation(position, orientation, speed_ratio)
- P1：未用 imports/参数 + 双扫描 + 返回格式 → 全修

## Section 9 审查 → FAIL → PASS
- P0：test_scripts_have_valid_paths 死断言 `or True` → 改为路径字符串 .py 后缀验证
- P1：3 个 stub 测试仅 assert isinstance(result, dict) → 改为验证 success/message/metrics

## Section 10 审查 → PASS
- P1：JSON 反序列化统一为字符串 + 新增 artifact 测试

---
tags: [experience-evolution, reflector, llm, review, gatekeeper]
date: 2026-05-19
summary: Gatekeeper 全量审查 feature/experience-evolution-system 分支，发现 P0×2 / P1×5 / P2×2，经验管道集成代码 2 个运行时崩溃
status: done
---

## [18:30] Gatekeeper 全量代码审查

- 用户要求：对 feature/experience-evolution-system 分支全量审查（30+ 文件）
- 审查内容：经验反馈链路（reflector → experience_manager → experience_filesystem → experience_reader）+ 核心修改（core.py, app.py）+ 回归检查（orchestrator, backends, llm, tools）
- 审查时间：约 8 分钟，64 次工具调用

### P0 — 阻塞发布（2个）

| # | 问题 | 位置 |
|---|------|------|
| 1 | `self._trigger_estop()` 未定义，`/estop` 紧急停止会 `AttributeError` | `app.py:679` |
| 2 | `self.provider` 未赋值，`_run_exp_manage()` 中 `Reflector(provider=self.provider)` 会崩溃 | `app.py:1070` |

### P1 — 应当修复（5个）

| # | 问题 | 位置 |
|---|------|------|
| 3 | `TOOL_CATEGORIES` 有 `"script"` 映射，但 `CATEGORIES` 无 `"script"`，写出的文件不会被 scan/merge/prune | `annotation.py:20` vs `experience_filesystem.py:17` |
| 4 | `physics_collector.py:72-76` 直接 `self._db.conn.execute()` 绕过 AuditDB 公共 API | `physics_collector.py` |
| 5 | `_delete_session_cascade` 引用已删除的 `experience_pending` 表（被 try/except 掩蔽） | `db.py:551` |
| 6 | `robocode/persistence/__init__.py` 零调用者的死重导出 shim | `persistence/__init__.py` |
| 7 | 物理数据 DB 写入失败静默吞掉，无操作者可见信号 | `physics_collector.py:85-86` |

### P2 — 建议（2个）

| # | 问题 | 位置 |
|---|------|------|
| 8 | `experience_manager.py` 方法体内重复 import（`write_experience`, `backup_before_update` 已在模块级导入） | `experience_manager.py:361-363,400-403,514-516` |
| 9 | `experience_reader.py:99` 畸形 confidence 值 `float()` 会 `ValueError`，被外层 try/except 静默吞掉 | `experience_reader.py` |

### 测试评估

- 单元测试覆盖良好（8 个测试文件，57+ tests），但 `_run_exp_manage()` 全流程集成测试缺失
- 两个 P0 bug 都藏在这个无测试覆盖的集成路径中

### 回归风险

- `app.py` 新增约 300 行（经验管道集成），风险最高
- `core.py` 改动向后兼容（可选参数 + `if self._xxx is not None` 守卫）
- orchestrator / backends / llm / tools 改动低风险

> **注意**：审查后用户已进行部分修改，上述 P0/P1 可能已有变动。当前状态以实际代码为准。

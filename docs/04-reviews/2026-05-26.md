---
tags: [review, safety, observability, evolvability, controllability, dead-code]
date: 2026-05-26
summary: 双层经验系统层1实现后全量审查 — 安全+可监测+可进化+可驾驭+可迭代+无用代码
status: done
---

## [20:00] 全量审查报告

审查范围：57 个 Python 源文件（robocode/ 全量）

### P0 — 必须修复（6 项）

| # | 文件:行 | 分类 | 问题 |
|---|---------|------|------|
| P0-1 | `codegen_tools.py:141-152` | 安全 | Sandbox env 白名单泄露 HOME/USER，子进程可推断系统布局 |
| P0-2 | `script_tools.py:216` | 安全 | 6D grasp instruction 未经校验直接做 CLI 参数 |
| P0-3 | `tool_guard.py:78-80` | 可驾驭 | L2 审批可通过 settings 配置绕过，无运行时确认 |
| P0-4 | `tool_guard.py:101-109` | 可驾驭 | "S"一键批准全部后续工具，物理机器人风险高 |
| P0-5 | `motion_tools.py:46` | 安全 | move_robot_xyz rotation 参数无边界校验 |
| P0-6 | `codegen_tools.py:15-39` | 安全 | FORBIDDEN_PATTERNS 可通过字符串拼接绕过 |

### P1 — 应当修复（16 项）

| # | 文件:行 | 分类 | 问题 |
|---|---------|------|------|
| P1-1 | `db.py:100` | 安全 | ALTER TABLE f-string 插值列名 |
| P1-2 | `db.py:604-607` | 安全 | DELETE FROM f-string 插值表名 |
| P1-3 | `core.py:208-209` | 可监测 | 硬件规格文件读取失败静默返回空串 |
| P1-4 | 全项目46处 | 可监测 | `except Exception:` 无日志 |
| P1-5 | `core.py:294` | 可监测 | checkpoint 保存失败不通知用户 |
| P1-6 | `physics_collector.py:73-76` | 可进化 | 直接写 SQL 绕过 DB 抽象层 |
| P1-7 | `wrapper_tools.py:14` | 可进化 | 模块级可变全局 _REGISTRY |
| P1-8 | `settings.py:11` | 安全 | api_key 默认空串，无启动校验 |
| P1-9 | `state_machine.py` | 可驾驭 | Orchestrator 类定义但未使用 |
| P1-10 | `approval.py:70-71` | 可驾驭 | should_prompt() 只检查 L2，与实际策略不一致 |
| P1-11 | `annotation.py:50` | 可监测 | tool_call_id=0 时静默跳过注册 |
| P1-12 | `experience_ui.py:188` | 可监测 | LLM 反思失败无详情 |
| P1-13 | `patch_tools.py:206-253` | 安全 | run_checks 无工作空间边界校验 |
| P1-14 | `app.py:235-236` | 可监测 | 启动清理失败完全静默 |
| P1-15 | `script_tools.py:6` | 可进化 | yaml 导入未验证依赖 |
| P1-16 | `reflector.py:37-46` | 安全 | 路径遍历防护用字符串前缀匹配，应用 is_relative_to |

### P2 — 建议（24 项）

包括：硬编码配置值、无用代码（safety.py 多个未调用方法、D3 variant、ROS2 template、persistence 重导出）、测试覆盖缺口（kinematics.py、tool_guard.py、slash.py 等 15 个模块无测试）

### 正面发现

- 无硬编码密钥、无 shell=True、无 eval/exec
- 良好的工作空间沙箱 + 受保护文件注册表
- 结构化日志 + 完整审计链
- 分层安全：正则扫描 + RLIMIT + -I 隔离

---

## [20:10] VLM 感知系统审查

审查范围：8 个新文件 + 6 个修改文件，约 500 行新增代码。

### P0 — 必须修复（2 项，已清零）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| P0-1 | `vlm-desktop-detection.md:26` | 硬编码 DashScope API Key (`sk-6e7e3...`) | 替换为 `sk-xxx`，清理 `_history/` 和 `_archive/` 备份 |
| P0-2 | 无测试文件 | VLM 感知系统零自动化测试 | 新建 `tests/test_perception.py`，24 用例全部通过 |

### P1 — 应当修复（8 项，已清零）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| P1-1 | `hooks.py:11` | EXPERIENCE_ROOT `.parent.parent.parent` 指向项目根而非 `robocode/` | 改为 `.parent.parent` |
| P1-2 | `core.py:323` | `_execute_hook` 的 `tool_input` 参数未使用 | 加 docstring 说明保留用途 |
| P1-3 | `vlm_perception.py:114` | 超时错误信息 `10s` 与实际 `15s` 不匹配 | 修正为 `15s` |
| P1-4 | `vlm_perception.py:102` | stdout JSON 解析只取最后一行 | 改为从末尾逐行回溯跳过非 JSON |
| P1-5 | `__init__.py:6` | `CaptureResult` 无外部引用却导出 | 从 `__all__` 移除 |
| P1-6 | `core.py:323` | `hook` 参数缺类型标注 | 加 docstring 注明为 `HookRule` |
| P1-7 | `fake_perception.py:25` | `locate` 用 `**kwargs` 违反 LSP | 改为显式参数签名 |
| P1-8 | `vlm_perception.py:119` | `_fake_capture` 在正常路径中为死代码 | 保留并文档化（支持直接 `sandbox=True` 用法） |

### 测试结果

- 新增: `tests/test_perception.py` — 24/24 PASSED
- 全量回归: 397/397 PASSED（零回归）

---

## [20:30] 双层经验系统层1功能审查

审查范围：`experience_reader.py`、`experience_ui.py`、`core.py`、`reflector.py` + 5 个经验 .md 文件

### 测试 & Lint

| 检查项 | 结果 |
|--------|------|
| pytest | **419 passed**, 0 failed |
| ruff check | All checks passed |
| ruff format | 2 files already formatted |

### 核心功能验证

| 组件 | 状态 | 验证结果 |
|------|------|----------|
| `experience_reader._build_tool_index()` | ✅ | 正确构建 9 个工具的反向索引 |
| `experience_reader.get_tool_tips()` | ✅ | 返回关联 tips 并记录 `used_files` |
| `core.py` tips 注入 | ✅ | 工具执行成功后自动追加 `💡 经验提醒` |
| `reflector.py` @tag 输出 | ✅ | prompt 已包含 `@tool_name` 格式说明和示例 |
| `experience_ui._merge_bullets_replace()` | ✅ | 三段冲突检测（>75%替换/55-75%待确认/<55%追加） |
| `experience_ui._decay_and_reinforce()` | ✅ | 所有经验 -0.01，使用的 +0.02 回涨 |
| `experience_ui._print_quality_dashboard()` | ✅ | 输出置信度统计 + tips 命中率 |

### 工具索引覆盖

| 工具 | tips 数量 |
|------|-----------|
| move_robot_joints | 12 |
| move_robot_xyz | 4 |
| get_robot_status | 5 |
| execute_command | 5 |
| generate_and_run_sdk_code | 4 |
| search_code | 2 |
| read_file | 2 |
| observe | 3 |
| locate | 2 |

### bullet 标签覆盖

| 文件 | 总 bullet | 有 @tag | 缺 @tag | 说明 |
|------|-----------|---------|---------|------|
| code-experience.md | 29 | 25 | 4 | 通用知识，可不加 |
| vlm-desktop-detection.md | 12 | 9 | 3 | 通用 CAUTION |
| 朝下判断.md | 5 | 1 | 4 | 纯知识，留给层2 |
| 状态检查.md | 1 | 1 | 0 | — |
| episode1-spec.md | 0 | — | — | 硬件文档，无 bullet |

### 残留问题

| # | 问题 | 影响 | 建议 |
|---|------|------|------|
| 1 | `code-experience.md` 有 4 条 bullet 缺 `@tool_name` | 低 | 第28/35/36/39行，属于通用知识，可不加 |
| 2 | `朝下判断.md` 有 4 条 bullet 缺 `@tool_name` | 低 | 第32-35行是纯知识，设计决策说"留给层2读书人" |
| 3 | `vlm-desktop-detection.md` 有 3 条 bullet 缺 `@tool_name` | 低 | 第26/29/31行是通用 CAUTION，可不加 |

### 结论

**代码质量良好，功能实现完整** ✅

- 层1 Push 模型 ✅ 完成
- 经验质量保障 ✅ 完成
- 测试全绿 ✅ 419/419
- 无 lint 错误 ✅

残留的 bullet 标签缺口都是通用知识类（不关联特定工具），按设计决策"留给层2读书人模式"处理，不阻塞。

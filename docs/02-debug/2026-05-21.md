---
tags: [test, fix, context-memory, experience-reader, voice, cli]
date: 2026-05-21
summary: 修复 10 个失败测试 —— ContextMemory 字段重命名、ExperienceReader 格式变更、Voice mock 不完整、CLI JSON 输出断言
status: done
---

## [18:50] 修复 10 个失败测试

- 用户要求：修复项目评价中发现的 10 个失败测试
- 改动文件: `tests/test_analytics.py`, `tests/test_experience_reader.py`, `tests/test_voice.py`, `tests/test_cli.py`
- 改动时间：~20 分钟

### 根因与修复

1. **test_analytics.py ×3**：`ContextMemory` 字段 `max_messages` → `max_tokens` 重命名，测试仍引用旧属性名 → 更新为 `max_tokens`
2. **test_experience_reader.py ×5**：index.md 解析格式从 `[category]` 改为 `|` 分隔符，测试数据仍是旧格式 → 更新 7 处测试数据为 `title | cat/file.md` 格式
3. **test_voice.py ×1**：mock device dict 缺少 `default_samplerate` 字段，`_detect_mic()` 评分函数 KeyError → 添加 `"default_samplerate": 16000`
4. **test_cli.py ×1**：`/resume` 无参返回 JSON 字符串（由 app.py UI 层渲染），测试仍检查中文 "会话" → 改为检查 `result2.action == "resume_select"`

### 结果

396 passed, 0 failed

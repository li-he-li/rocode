---
tags: [experience-evolution-system, manager, merge, prune, logging, gatekeeper]
date: 2026-05-14
summary: 补齐 ExperienceManager 缺失功能（update/merge/prune）、修复 process_pending 空操作、接入 /exit 直接触发、实现置信度反馈闭环、补全决策过程日志、接入 startup cleanup 和 requeue
status: done
---

## 缺口审计与补齐

### 审计发现（Milestone 2 & 3）

| 计划项 | 审计前 | 修复后 |
|--------|--------|--------|
| update_experience() | 未实现 | 读取旧 FM → 合并更新 → backup → write |
| merge_experiences() | 未实现 | difflib 相似度 >80% → archive 源文件 → 合并写入 |
| prune_experiences() | 仅 should_prune 判定 | 全量扫描 → archive → rebuild_index |
| _process_one_pending() | 空操作（结果丢弃） | analyze → create → write → rebuild_index |
| /exit 直接触发 | 仅标注面板间接触发 | exit_requested + exit_with_annotations 两路均触发 |
| 置信度反馈闭环 | 缺失 | 标注后 ±0.03/0.05 更新对应分类 confidence |
| /stats 物理/标注列 | 缺失 | recent_sessions_with_stats SQL → render_session_list 表格 |
| startup 中断清理 | DB 方法存在但未调用 | _startup_cleanup() 在 init 阶段调用 |
| requeue_failed_pending | DB 方法存在但未调用 | _startup_cleanup() 中调用 |
| 决策过程日志 | 仅有结果日志 | 5 个方法增加 _start → decision → _done 三段式日志 |

### 决策日志清单

新增 12 条结构化日志事件：

- physics_analysis_start / physics_analysis_done — 数据量、工具分布、跳过原因
- annotation_processing_start / annotation_processing_done — 分类统计、高频失败原因
- call_flow_analysis_start / call_flow_analysis_done — 任务数、序列模式、重试检测
- experience_created — 类型/置信度 + 赋值理由（rationale）
- merge_scan_start / merge_decision / merge_scan_done — 相似度、合并理由
- prune_scan_start / prune_decision / prune_scan_done — 候选详情、淘汰理由

### 测试结果

82 passed, 0 failed, ruff lint clean.

### 待 Gatekeeper 审查

已触发 enterprise-code-gatekeeper 后台审查。

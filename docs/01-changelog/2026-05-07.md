---
tags: [voice, stt, faster-whisper, analytics, metrics, logging, audit, session, checkpoint, resource-tracking]
date: 2026-05-07
summary: voice-stt-control（语音引擎 + CLI 集成）+ monitorable-robust-analytics（全系统可监测架构：结构化日志/指标/审计增强/LLM 元数据/会话持久化/资源追踪）
status: done
---

# 2026-05-07 — 功能变更

## voice-stt-control Phase 1-4 实现
- 新建 robocode/cli/voice.py（~230行）：VoiceState 枚举、VoiceController 类
  - 麦克风检测、faster-whisper 模型加载（GPU CUDA float16 / CPU int8 自动检测）
  - sounddevice InputStream 录音、webrtcvad 3s 静音检测
  - 后台转录、回调机制、shutdown 资源释放
- 修改 robocode/cli/app.py：集成 VoiceController（F2 KeyBinding、bottom_toolbar、banner、_on_voice_result 回调）
- 修改 robocode/cli/slash.py：/audit 增加语音操作统计行
- 修改 pyproject.toml：增加 [project.optional-dependencies] voice 组
- 无硬依赖：未安装 voice 依赖时状态为 UNAVAILABLE

## monitorable-robust-analytics（~57/65 任务完成）

### Section 1: 基础设施
- 新建 robocode/services/analytics/ 包（6 个文件）
- logger.py：structlog 双写（JSON Lines + Rich 控制台），RotatingFileHandler
- db.py：从 persistence/db.py 迁移，新增 auto-migration + 聚合查询方法
- persistence/db.py → 兼容性重导出

### Section 2: 全模块日志统一
- 9 个模块从 structlog.get_logger() → get_logger("module")

### Section 3: 指标收集
- metrics.py：MetricsCollector — record() 计数器、record_latency() 直方图、timer() 上下文管理器
- tool_guard.py → metrics.record("safety_rejection") + 修复 3 处静默 except: pass

### Section 4: 审计 DB 增强
- _auto_migrate(): 启动时检测并 ALTER TABLE 添加 duration_ms/ended_at 列
- 聚合查询：tool_latency_stats(avg/min/max/p95/P50)、tool_success_rate、session_summary

### Section 5: LLM 元数据
- llm/base.py: StreamEvent.kind 新增 "metadata"
- deepseek_provider.py: 流结束时 emit metadata 事件（model, latency_ms, completion_chars, token估计）

### Section 6: 会话持久化
- agent/context.py: ContextMemory.to_json()/from_json() 序列化
- agent/core.py: _save_checkpoint() 存储完整上下文
- cli/slash.py: /resume 升级 — 列表显示调用数/成功率；恢复上下文
- cli/app.py: _restore_session() + _check_unclosed_sessions()

### Section 7: 资源追踪
- resource_tracker.py: ResourceTracker — track_file/track_process 注册、cleanup() 清理、detect_dirty_state()

### Section 8: /audit 增强
- display.py: Rich Table 渲染（render_session_list, render_tool_stats, render_safety_stats）

### Section 9: 语音监控集成
- VoiceController 接受 MetricsCollector 注入 → record_stt_result() 同步数据

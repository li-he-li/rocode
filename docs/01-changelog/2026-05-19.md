---
tags: [experience-evolution, reflector, readability, merge, security, tests]
date: 2026-05-19
summary: Reflector + 安全加固 + 经验系统修复 — 355 tests passed
status: done
---

## [15:30] 新建 agent/reflector.py

- `Reflector` 类：接收规则层结构化摘要，单次 LLM 调用产出 3-8 条 delta bullets
- `_build_reflection_context()`：将 physics/annotations/call_flows 拼接为 LLM 可读上下文
- `_extract_bullets()`：支持 5 种 LLM 输出格式（标准/数字列表/加粗/逆序/fallback）
- `deduplicate_bullets()`：SequenceMatcher 文本去重，阈值 0.75

## [15:35] 修改 experience_manager.py

- `create_experience()` 新增 `bullets` 参数
- 规则建议和 LLM bullets 合并到单一 `## 建议` 章节
- 新增 `extract_existing_bullets()` 从已有文件提取 bullets 供去重
- 新增 `_merge_bodies()` 合并两份 body 时去重 bullets
- 新增 `_tool_label()` / `_CAT_LABELS` 工具名中文注释映射
- `_render_annotations()` 小节标题加中文标签

## [15:40] 修改 cli/app.py — _run_exp_manage

- 重构为三步管线：规则分析 → LLM 反思 → 写文件时按工具/类别分配 bullets 并去重
- Reflector 失败不阻塞（try/except 跳过）

## [15:45] 修改 experience_filesystem.py — rebuild_index

- 新增 `_extract_title_from_file()` 从文件 body 提取中文标题
- index.md entry 格式改为 `- [cat] 中文标题 | filename.md (confidence=X)`
- bullets 提取改为从 `## 建议` 段落（不再从 `## 反思洞察`）

## [15:50] 修改 experience_reader.py — 解析新索引格式

- 正则支持新旧两种 entry 格式（带 title | filename 和旧 filename-only）
- 按缩进区分 entry 行和 bullet 子行
- `get_index_summary()` 渲染 bullets 到 SYSTEM_PROMPT

## [16:00] 新建 tests/test_reflector.py

- 24 个测试：context 构建(5) + bullet 提取(7) + 去重(5) + mock reflect(3) + merge bodies(4)

## [16:10] 最终验证

- 57 tests passed / 0 failed
- 语法检查全部通过
- 完整链路验证：经验文件 → index.md → ExperienceReader → SYSTEM_PROMPT 注入

## [16:20] Gatekeeper 审查修复

### P0 修复
- `_trigger_estop()` 未定义 → 新增方法，调用 `self.backend.emergency_stop(True)`
- `self.provider` 未赋值 → 改为 `self.agent.provider`（provider 只存在 agent 实例上）

### P1 修复
- CATEGORIES 缺 "script" → 添加到列表
- `_delete_session_cascade` 引用已删除表 → 清理为只保留实际存在的 5 个表

### P2 修复
- confidence 解析不防错 → 新旧两种格式均加 try/except ValueError

### 记录（原有问题，非本次引入）
- P1-4: physics_collector.py 直接访问 self._db.conn
- P1-6: persistence/__init__.py 死重导出
- P1-7: 物理数据 DB 写入失败静默吞异常
- P2-8: experience_manager.py 方法体内重复 import

## [16:50] 安全审查修复

### P0 修复
- exec_tools.py: `shell=True` → `shlex.split()` + `shell=False` + 元字符正则拦截 (`| ; ` $ > & <`)
- codegen_tools.py: 沙箱加固 — 移除 header 中 `import os`、拦截 `__subclasses__`/`__builtins__`/`getattr` 绕过、`preexec_fn` 设 RLIMIT_AS=512MB + RLIMIT_NPROC=0、环境变量白名单

### P1 修复（确认后修正）
- protected_files.py: 审查者说缺 `agent/core.py`，实际列表第25行已有 → 不存在此问题
- safety.py: 审查者说 `check_speed` 不拦截负值，实际 `<=0` 已拦截 → 不存在此问题
- deepseek_provider.py: `thinking:disabled` 硬编码 → 加 `Settings.provider.thinking_enabled` 配置项
- db.py: `json_extract($.success) = 1` 脆弱 → 改为 `IN (1, 'true')` 兼容 int/str

### 额外修复
- display.py: `Panel(str(table))` 导致 Rich Table 不渲染 → 改为 `Panel(Group(*lines))`

## [17:00] 经验系统数据流修复 — 经验文件不生成根因

### P0 修复
- `list_tool_calls("")` WHERE session_id="" 永远返回空 → 加 `if session_id:` guard，空字符串时返回全量
- Ctrl+C/EOF 退出直接 break 不跑 `_run_exp_manage()` → break 前加 `await self._run_exp_manage()`

### P1 修复
- `cleanup_interrupted_session` 删掉未标注物理数据 → 改为只标记会话状态为 closed，数据保留供经验管理消费
- 物理数据阈值太高（3 per tool, 2 per speed group）→ 降到 2 per tool, 1 per speed group

### P2 修复
- 无 processed 标记导致数据重复处理 → `_auto_migrate` 加 `processed` 列、`get_unprocessed_*` 只查 `processed=0`、写入后调 `mark_physics_processed()` / `mark_annotations_processed()`
- 更新 test_experience_retry.py 适配新行为

### 最终验证
- 355 passed / 1 failed (pre-existing voice mic test) / 0 regressions

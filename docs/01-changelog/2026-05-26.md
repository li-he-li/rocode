---
tags: [experience, tool-guard, reflector, auto-inject]
date: 2026-05-26
summary: 实现双层经验系统层1 — 工具调用时自动注入经验提醒（Push模型）
status: done
---

## [19:00] 双层经验系统层1实现

- 用户要求：对比计划文档，实现工具调用时自动注入经验提醒
- 改动内容：
  - `robocode/agent/experience_reader.py`：新增 `_build_tool_index()`、`get_tool_tips()`、`reload()` 方法，从 bullet 的 `@tool_name` 标注构建反向索引
  - `robocode/agent/reflector.py`：REFLECTOR_SYSTEM_PROMPT 增加 `@tool_name` 输出格式说明和示例
  - `robocode/agent/core.py`：`_execute_tool()` 成功路径追加经验 tips 到 tool_result message
  - 4 个经验 .md 文件：19 条 bullet 加 `@tool_name` 标注（覆盖所有提及工具名的 bullet）
- 改动时间：2026-05-26 19:00

## 设计决策

- tips 注入点选在 `core.py` 而非 `tool_guard.py`：批准和拒绝路径都能看到 tips，且不需要改 ToolGuard 接口
- `_build_tool_index()` 在 `_load_index()` 之后调用，确保 entries 已加载
- `reload()` 方法同时重建 entries 和 tool_tips 索引
- 现有 25 条纯知识 bullet 不加 `@` 标注（留给未来的层2读书人模式）

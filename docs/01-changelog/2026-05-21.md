---
tags: [review, bugfix, regression, refactor, cleanup, app, split, tools, experience, test, hardware, codegen, skills, verification]
date: 2026-05-21
summary: 项目评价 Top 3 改进全完成 — Gatekeeper 审查 + 10 测试修复 + FakeEpisodeAPP 去重 + app.py 拆分 (1414→679行)
status: done
---

## [14:30] Gatekeeper 审查 — hardware spec + 臂展修正

- 用户要求：对 robocode/ 改动进行独立质量门控审查
- 审查文件: `robocode/agent/core.py`（路径修正）, `robocode/experience/hardware/episode1-spec.md`（文档扩展）
- 审查结果: **PASS** ✓（P0×0 P1×0 P2×2）
- P2#1 臂展 447mm → 507mm（补齐 J3→J4 段长 ~60mm）已修复
- P2#2 建议为 `_load_hardware_spec` 添加烟雾测试，既有的技术债
- 文档: `docs/04-reviews/2026-05-21.md`

## [18:50] 修复 10 个失败测试

- 用户要求：修复项目评价发现的 10 个失败用例
- 根因与修复:
  - `test_analytics.py` ×3: `ContextMemory` 字段 `max_messages` → `max_tokens`，测试未同步
  - `test_experience_reader.py` ×5: index.md 格式改为 `|` 分隔符，测试数据仍是旧 `[category]` 格式
  - `test_voice.py` ×1: mock device 缺 `default_samplerate` → 补充 + 竞态放宽（READY/LOADING 均可）
  - `test_cli.py` ×1: `/resume` 无参返回 JSON，测试断言查中文 → 检查 `action == "resume_select"`
- 结果: **396/396 全绿**
- 文档: `docs/02-debug/2026-05-21.md`

## [19:05] FakeEpisodeAPP 去重

- 用户要求：消除 codegen_tools.py 中的 `_SandboxEpisodeAPP` 手工副本
- 方案: `_build_sandbox_header()` 用 `inspect` 从 `FakeEpisodeAPP` 反射生成沙箱类
- 改动: `robocode/tools/codegen_tools.py`
  - 删除 ~24 行手写 `SANDBOX_HEADER` 字符串
  - 新增 ~45 行 `_build_sandbox_header()` 函数
  - 自动补全了手工版遗漏的 `move_linear_xyz_rotation` + `set_free_mode`
- 结果: 沙箱头自动与 `FakeEpisodeAPP` 保持同步，永无漂移

## [19:20] cli/app.py 拆分重构

- 用户要求：按项目评价 #3，拆分 1414 行的 cli/app.py
- 新建文件:
  - `cli/tools_setup.py`（477 行）：`register_all_tools(app)` + `build_handler_map(app)`
  - `cli/experience_ui.py`（376 行）：`_run_exp_manage(app)` + 标注面板 + 置信度反馈 + bullet 合并
- 保留文件: `cli/app.py` → 679 行（-52%）
- 技术手法: Python descriptor protocol — 外部函数第一参数是 `app`，通过类属性赋值变成 bound method
- 结果: 396/396 测试通过，RobocodeApp 构造正常

## [19:45] build_handler_map 回归修复

- 问题: 拆分时错误地把 `_build_handler_map` 从工厂函数调用改成了手写 handler，导致:
  - `move_robot_joints` 调 `backend.move_joints()` → `SdkBackend` 无此方法
  - `execute_command` 等调 `safety.assert_not_estopped()` → 脱离原有设计
- 修复: `build_handler_map` 改回使用 `make_motion_tools` / `make_gripper_tools` / `make_script_tools` 等工厂函数聚合 handler
- 附加: `list_skills` 恢复原格式（含结构化 metrics）

## [19:50] 冗余代码清理

- `tools_setup.py`: 删除 `register_all_tools` 内重复的 `from robocode.tools.script_tools import SCRIPT_INVENTORY`
- `experience_ui.py`: 删除未使用导入 `import json` + `from rich.panel import Panel`
- `app.py`: 删除死代码 `_merge_bullets_replace = staticmethod(...)`（仅模块内部调用）

## [19:55] 进化 + Skills 系统验证

- 经验系统: 4 条经验正常加载，index summary 1444 字符，硬件描述注入 system prompt ✓
- Skills: 23 handler（含 15 个技能），`list_skills` 返回结构化数据（类别/人工标记）✓
- AgentLoop system prompt: 6219 字符，含经验区块 + 硬件描述 ✓

---
tags: [experience-evolution, reflector, llm, prompt, merge, hardware, refactor]
date: 2026-05-20
summary: 经验进化系统大幅重构 — Reflector prompt 重写 + 扁平化分类 + LLM 驱动合并 + 硬件描述注入 + sandbox 放宽
status: done
---

## [16:00] Reflector prompt 重写 — 因果规律优先
- 用户要求：经验固化质量太差，产出的 bullets 是空洞的复述
- 改动：重写 `REFLECTOR_SYSTEM_PROMPT`，新增知识优先级（A因果关系 > B可复现参数 > C禁止事项），新增 RULE 类型标签
- 改动文件：`robocode/agent/reflector.py`

## [16:20] Reflector prompt 增加意图标签
- 用户要求：标签太死板，增加用户意图标签
- 改动：格式从 `- [类型]` 改为 `- [意图|类型]`，新增 `_normalize_tag()` 和 `_extract_bullets()` 多格式解析
- 改动文件：`robocode/agent/reflector.py`

## [16:30] 经验文件清理 + VLM 桌面检测经验重写
- 用户要求：清理垃圾经验文件，重写为有价值的内容
- 改动：删除 5 份垃圾文件（motion/script/code-experience/2个physics），新建 `vlm-desktop-detection.md`
- 改动文件：`robocode/experience/code/vlm-desktop-detection.md`

## [16:40] move_robot_xyz 陷阱发现
- 用户反馈：Agent 总是用 xyz 坐标而不是关节角度
- 发现：`motion_tools.py:46` 的 `move_robot_xyz` 默认 `rotation=(180,0,90)`，只传 x/y/z 会重置姿态
- 改动：更新 `code-experience.md` 加入 CAUTION 和正确做法
- 改动文件：`robocode/experience/code/code-experience.md`

## [16:50] 硬件描述注入 SYSTEM_PROMPT
- 用户要求：Agent 对机械臂理解为 0，需要硬件参数
- 改动：从 URDF/SDF + safety.py + settings.py 提取参数，新建 `experience/hardware/episode1-spec.md`，注入 `core.py` SYSTEM_PROMPT
- 改动文件：`robocode/experience/hardware/episode1-spec.md`、`robocode/agent/core.py`

## [17:00] 扁平化经验分类
- 用户要求：分类没有价值，干掉它
- 改动：删除 `CATEGORIES` 列表，文件从子目录移到根目录，删除空目录和 `_index.md`，重写 `rebuild_index()` 扫描扁平结构
- 改动文件：`robocode/agent/experience_filesystem.py`、`robocode/agent/experience_manager.py`、`robocode/cli/app.py`

## [17:10] _run_exp_manage 重写 — 单文件 + 去重反馈
- 改动：每次会话只生成 1 个文件（不按 category 拆），transcript 不全文写入，用户反馈去重后截断 500 字
- 改动文件：`robocode/cli/app.py`

## [17:20] physics_collector NoneType bug 修复
- 用户反馈：`capture_after` 报 `TypeError: unsupported operand type(s) for -: 'NoneType' and 'float'`
- 修复：`get_physics_summary()` 增加 `if None in before_angles or None in after_angles: return None`
- 改动文件：`robocode/agent/physics_collector.py`

## [17:30] _summarize_params 恢复
- 用户反馈：`NameError: name '_summarize_params' is not defined`
- 原因：函数被误删（git stash 与 HEAD 差异）
- 修复：恢复 `_summarize_params` 函数定义
- 改动文件：`robocode/agent/core.py`

## [18:00] sandbox 放宽 write_text/write_bytes
- 用户反馈：`generate_and_run_sdk_code` 的写文件操作全被拦截，VLM 检测无法写脚本到 /tmp
- 改动：从 `FORBIDDEN_PATTERNS` 中移除 `\.write_text\(` 和 `\.write_bytes\(`，os/subprocess 仍拦截
- 改动文件：`robocode/tools/codegen_tools.py`

## [18:10] bullets 合并阈值 + 相似度匹配
- 改动：新增写入前 bullets 重叠率匹配（阈值 40%），超过则合并到已有文件而非新建
- 改动文件：`robocode/cli/app.py`

## [18:30] Reflector prompt 标签放开 + 示例多样化
- 用户要求：标签类型放开，增加示例多样性
- 改动：格式从 `- [意图|类型]` 简化为 `- [意图]`（自由定义），示例从 6 条扩展到 11 条覆盖 4 类场景
- 改动文件：`robocode/agent/reflector.py`

## [18:40] Reflector 可见已有经验索引
- 用户要求：Reflector 应该看到已有经验，避免重复学习
- 改动：`_build_reflection_context` 新增 `experience_index` 参数，传入 `index.md` 摘要（~600 tokens），不加载全部文件
- 改动文件：`robocode/agent/reflector.py`、`robocode/cli/app.py`

## [19:00] LLM 驱动合并 — 进化保护机制
- 用户要求：经验进化不能退化，盲目覆盖不行，交给 LLM 决定
- 改动：
  - prompt 新增更新指令：`→ 更新:xxx.md` 标记
  - 新增 `_parse_bullet()` 和 `extract_bullets_with_targets()` 解析结构化返回
  - `Reflector.reflect()` 返回 `list[dict]`（含 intent/content/update_target）
  - `_run_exp_manage` 按 LLM 的 update_target 分组处理：有目标→合并到指定文件，无目标→新建
  - confidence 改为 `min(old+0.03, 0.95)` 只升不降
- 改动文件：`robocode/agent/reflector.py`、`robocode/cli/app.py`

## [20:20] 更新时替换旧 bullets（防退化 P0）
- 问题：LLM 标记 `→ 更新:xxx.md` 后，新 bullets 是追加到 `## 建议` 末尾，不删除旧的。导致矛盾规则共存（如"J5 必须减小"和"J5 必须增大"同时出现）
- 修复：新增 `_merge_bullets_replace()`，新 bullet 与已有 bullet 相似度 >55% 时**替换**旧 bullet，不相似则追加。整个 `## 建议` 段重建
- 改动文件：`robocode/cli/app.py`

## [19:15] Reflector prompt 精简 — 质量>数量原则
- 改动：prompt 新增"质量 > 数量"和"可操作 > 抽象"原则，新增"不要输出已有索引中已有的内容"
- 改动文件：`robocode/agent/reflector.py`
